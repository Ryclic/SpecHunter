# Paper

`main.tex` and `main.bib` build `spechunter_a3_2026.pdf` (IEEEtran two-column, limit 4 pages).
Build it with `./build_main.sh`, which uses Tectonic or latexmk, or upload both source files to
Overleaf.
